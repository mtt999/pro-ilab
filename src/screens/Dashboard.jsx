// Placeholder — large screen files will be migrated in the next session
export default function Dashboard() {
  return <div style={{ padding: 32, color: 'var(--text)' }}>Dashboard — pending full migration from lab-inventory base.</div>
}
